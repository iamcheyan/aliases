# Git ツールエイリアス
alias easygit="bash ${HOME}/.dotfiles/tools/easygit.sh"
alias lg="lazygit"
alias gstsh="git status --short"
alias g:a="git add -A"
alias g:u="git reset HEAD"
alias g:w="git stash"
alias g:p="git stash pop"
alias g:s="git status -s"
alias g:ui="gitui"
git:ui() { command gitui "$@"; }
git:delta:init() {
    if ! command -v delta >/dev/null 2>&1; then echo "delta 未安装"; return 1; fi
    git config --global core.pager delta
    git config --global interactive.diffFilter 'delta --color-only'
    git config --global delta.navigate true
    git config --global delta.side-by-side true
    echo "✓ 已启用 delta"
}
git:diff() { command git diff "$@"; }
git:init() { easygit init "$@"; }
alias git:clean="bash ${HOME}/.dotfiles/scripts/dev/git_clean.sh"
alias dotfiles:push="bash ${HOME}/.dotfiles/scripts/dev/push_dotfiles.sh"
